# Base URL of the .NET backend (the "http" launch profile in backend/Properties/launchSettings.json).
# Leave empty to call the same origin instead — use that with the Vite proxy in vite.config.ts.
VITE_APP_API='http://localhost:5005'